[build]
  command = "npm install -g pnpm && pnpm install && pnpm --filter @workspace/admin-dashboard run build"
  publish = "artifacts/artifacts/admin-dashboard/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
